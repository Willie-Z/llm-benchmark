# 星流 LLM 多路 Endpoint 验收对比工具

> ⚠️ **当前阶段声明**：本仓库目前是**规格包 + 数据 + 演示原型**，**不是可直接交付做正式验收的成品**。
> - `prototype/` 是**可交互设计稿**：UI/交互/基线展示是真实的，但**验收结果(✅❌)为前端模拟值**，**未连接后端、不真实调用 LLM**。要让结果真实，需**后端配合接入**。
> - `baselines/`、`catalog.json` 是**真实可用数据成果**（带官网来源，已核实）。
> - `docs/` 是**规格文档**（PRD 待评审、主规格喂AI、拆解报告作蓝本），需按评审更新。
> - 待办：后端接入（CORS/鉴权/多路接口）、按客户标准生成用例的落地、部署到可访问地址。
> 详见 `docs/PRD.md` §7「待拍板项」与本文「当前阶段与待办」。

---

## 让 AI 1:1 复刻参考验收站点

## 仓库结构

```
docs/         PRD、建站主规格、参考网站拆解报告（喂给 AI 的规格）
baselines/    5 家官方参数基线 JSON（DeepSeek/智谱/MiniMax/Kimi/MiMo）
data/         参考站点数据：27 测试用例(bench_plan)、运行结果(bench_runs)、聚合模型树(catalog)、参考前端源码
prototype/    可运行原型：双击 prototype.html 即开
```

## 快速开始

1. **看最终网站长什么样** → 打开 `prototype/prototype.html`（需同目录 `_data.js`）
2. **喂 AI 生成正式网站** → 先读 `docs/建站主规格-面向AI生成.md`（含喂料顺序），再读 PRD 与拆解报告
3. **基线数据** → `baselines/*.json` 每条带官网来源，"官网未公布"项标 NOT_PUBLISHED

## 关键说明

- **基线各自独立**：各模型按各自官方信息建基座，不强套统一模板；官网未公布的项（如 Kimi 最大输出、各厂 benchmark）不测，判 INC/SKIP。
- **限速口径各家不同**：DeepSeek=并发数、智谱=并发数(已登录补全)、MiniMax=档位、Kimi=充值档、MiMo=RPM/TPM。
- **判定四态**：✅ PASS / ❌ FAIL / ⚠️ INCONCLUSIVE（官网未公布）/ ⏭️ SKIP（该项不适用）。
- 参考站点 `120.92.93.22/llm-benchmark` 为公司自有系统，后端 API（`/api/plan`、`/api/runs`）公开可调，本工具直连复用。

## 两个增量

1. **多路 Endpoint 并排对比**：同一用例多路并发，横向对比一致性 + 纵向对比官方对齐。
2. **按客户测试标准生成新用例**：结合基线 + 四类对齐维度，产出结构化用例并入用例树。

详见 `docs/PRD.md`。

---

## 当前阶段与待办（务必先读，避免误以为"已完成"）

### 各文件实际用处
| 文件 | 实际用处 | 局限 |
|---|---|---|
| `baselines/*.json`、`catalog.json` | **真实可用数据**，作为官方标准，前后端直接用 | 静态数据，需人维护更新；个别项官网未公布(已标) |
| `prototype/prototype.html` | **可交互设计稿**，演示最终网站长啥样、交互、基线展示 | **验收结果为模拟值(假)、未连后端、不真调LLM、非成品** |
| `data/bench_plan.json`、`bench_runs.json` | 参考站**真实数据样本**，照着写/改后端接口结构 | 是参考站快照，非本仓库产出 |
| `docs/*` | 规格文档：PRD(评审)、主规格(喂AI)、拆解(蓝本) | 文档非实现，需按评审更新 |

### 待办（让原型变真成品的关键，需人配合）
1. **后端接入**（最关键）：前端要能调 `120.92.93.22/llm-benchmark/api/runs`，需确认 CORS、鉴权 token、是否能多路并发——**需后端/运维配合**。
2. **判定接真实接口**：把 `prototype.html` 里 `launchBtn` 的模拟判定，换成对 `/api/runs` 的真实调用。
3. **多路接口**：参考站现接口是单路跑测，多路对比可能需后端加编排接口（见 PRD §7.2 第4项）。
4. **客户标准生成用例**：落地生成逻辑（PRD §4 模块三3.2）。
5. **部署到可访问地址**：公司服务器(能连真后端，最稳) 或 公开仓GitHub Pages(只能演示)。

> 结论：展示**设计稿+真实基线数据+规格**，但需知"结果是模拟，待后端接入"。
